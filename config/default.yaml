# yaml-language-server: $schema=./config.schema.json
logging:
  level: info
  request: forbidden
  database: warn
network:
  trustProxy: false
database:
  url: postgresql://depixy:depixy@db:5432/depixy
