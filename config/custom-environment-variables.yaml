logging:
  level: LOGGING_LEVEL
  request: LOGGING_REQUEST
  database: LOGGING_DATABASE
network:
  trustProxy:
    __name: NETWORK_TRUST_PROXY
    __format: boolean
  ipHeader: NETWORK_IP_HEADER
database:
  url: DATABASE_URL
session:
  secret: SESSION_SECRET
  salt: SESSION_SALT
  expiry:
    __name: SESSION_EXPIRY
    __format: number
