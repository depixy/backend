logging:
  level: LOGGING_LEVEL
  request: LOGGING_REQUEST
  database: LOGGING_DATABASE
network:
  trustProxy: NETWORK_TRUST_PROXY
database:
  url: DATABASE_URL
session:
  secret: SESSION_SECRET
  salt: SESSION_SALT
